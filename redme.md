we can select elements id by getElementById
we can select elemets classes using getElementsByClassName
we can get the first query by querySelector 
we can get all query by querySelectorAll
we can create element by createElement
event bubble is like a tree structure.it propogates to the top.
event delegation is we can effictivily attach single event listener to a parent element rather than multiple listeners to individual child elements
prevent default stop reload
and top propagating does not bubble